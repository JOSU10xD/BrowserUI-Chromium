
<Window
    x:Class="BarcodeWebview.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarcodeWebview"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Button to Toggle Capture -->
        <Button Grid.Row="0" Content="Toggle Capture" Click="Button_Click" />

        <!-- TextBox to Display Decoded QR Code -->
        <TextBox x:Name="textBox" Grid.Row="1" IsReadOnly="True" PlaceholderText="Scanned QR Code will appear here..." />

        <!-- MediaPlayerElement for Camera Preview -->
        <MediaPlayerElement
            x:Name="player"
            Grid.Row="2"
            AutoPlay="True"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Stretch="Uniform" />
    </Grid>
</Window>
