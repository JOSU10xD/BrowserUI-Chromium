<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="BrowserUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BrowserUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="BrowserUI">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Top Bar -->
        <StackPanel Orientation="Horizontal">
            <Button Content="Back" Click="BackButton_Click" />
            <Button Content="Forward" Click="ForwardButton_Click" />
            <Button Content="Refresh" Click="RefreshButton_Click" />
            <Button Content="Home" Click="HomeButton_Click" />
            <ComboBox x:Name="UrlBox" IsEditable="True" Width="400" Loaded="UrlBox_Loaded" KeyDown="UrlBox_KeyDown" PlaceholderText="Search..">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Content="Go" Click="GoButton_Click" />
            <Button Content="Settings" Click="SettingsButton_Click" />
            <Button Content="Screenshot" Click="ScreenshotButton_Click" />
        </StackPanel>

        <!-- Tab Control -->
        <controls:Pivot x:Name="TabPanel" Grid.Row="1" SelectionChanged="TabPanel_SelectionChanged">

            <!-- Home Tab -->
            <controls:PivotItem Header="Homepage" Name="HomeTab">
                <Grid Background="LightCyan">
                    <TextBox Name="SearchBar" Width="600" HorizontalAlignment="Center" VerticalAlignment="Center" PlaceholderText="Search or enter URL" />
                </Grid>
            </controls:PivotItem>

            <!-- Add Tab Button -->
            <controls:PivotItem Header="+" Name="AddTabButton">
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Add a new tab" />
            </controls:PivotItem>

        </controls:Pivot>

        <!-- WebView2 Browser -->
        <controls:WebView2 x:Name="WebView" Grid.Row="1" />
    </Grid>
</Window>
    