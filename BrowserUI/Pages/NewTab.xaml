<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BrowserUI.Pages.NewTab"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BrowserUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="RootGrid">
        <!-- Wallpaper Image -->
        

        <!-- Home Screen Content -->
        <Grid x:Name="HomeScreenGrid" Visibility="Visible">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                <TextBlock x:Name="NtpTime" FontSize="48" HorizontalAlignment="Center" />
                <TextBlock x:Name="NtpDate" FontSize="24" HorizontalAlignment="Center" />

                <AutoSuggestBox x:Name="SearchBox" PlaceholderText="Search or enter URL"
                                QuerySubmitted="SearchBox_QuerySubmitted" Width="400" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
                    <Button Content="Toggle Date" Click="ToggleDateButton_Click" />
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- WebView for browsing -->
        <WebView2 x:Name="BrowserView" Visibility="Collapsed" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />

    </Grid>
</Page>
