<Window
    x:Class="qrcode.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:qrcode"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Button to Toggle Capture -->
        <Button 
            x:Name="ToggleCaptureButton" 
            Grid.Row="0" 
            Content="Toggle Capture" 
            Click="Button_Click" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"
            Margin="10" />

        <!-- TextBox to Display Status or QR Code Link -->
        <TextBox 
            x:Name="textBox" 
            Grid.Row="1" 
            IsReadOnly="True" 
            PlaceholderText="Scanned QR Code will appear here..." 
            Margin="10" 
            HorizontalAlignment="Stretch" />

        <!-- MediaPlayerElement for Camera Preview -->
        <MediaPlayerElement
            x:Name="player"
            Grid.Row="2"
            AutoPlay="True"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Stretch="Uniform"
            Margin="10" />
    </Grid>
</Window>
